import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MiGranjaHidroponica',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
